Perfect 🔥 now we’ll build a **real CI/CD pipeline with auto-deploy**

👉 Goal:

```text
GitHub push → Jenkins auto trigger → Build → Test → Docker → Deploy 🚀
```

---

# 🚀 1. Overall Flow (Understand First)

```text
You push code →
GitHub Webhook →
Jenkins Pipeline triggers →
Build + Test →
Docker build →
Deploy on server
```

---

# 🧱 2. Prerequisites

Make sure you have:

* ✅ GitHub repo (your project)
* ✅ Docker installed
* ✅ Jenkins running (via Docker ✅)
* ✅ Port `8080` open

---

# 🚀 3. Install Required Jenkins Plugins

👉 Go to:

```text
Manage Jenkins → Plugins
```

Install:

* Git Plugin
* Pipeline
* Docker Pipeline
* GitHub Integration

---

# 🚀 4. Create Jenkins Pipeline Job

---

## Steps:

1. Go to Jenkins Dashboard
2. Click **New Item**
3. Name: `MyApp-Pipeline`
4. Select: **Pipeline**

---

## Configure:

### 🔹 Pipeline Section

```text
Definition → Pipeline script from SCM
```

Fill:

```text
SCM → Git
Repository URL → your GitHub repo
Branch → main
Script Path → Jenkinsfile
```

---

# 🚀 5. GitHub Webhook (AUTO TRIGGER 🔥)

---

## Step 1: Copy Jenkins URL

```text
http://your-ip:8080/github-webhook/
```

---

## Step 2: Add Webhook in GitHub

👉 Go to your repo:

```text
Settings → Webhooks → Add Webhook
```

Fill:

* Payload URL:

```text
http://your-ip:8080/github-webhook/
```

* Content type:

```text
application/json
```

* Trigger:

```text
Just the push event
```

---

## Step 3: Enable in Jenkins

👉 In pipeline job:

```text
Build Triggers → ✔ GitHub hook trigger
```

---

# 🚀 6. Final Jenkinsfile (Production Ready)

```groovy
pipeline {
    agent any

    environment {
        IMAGE_NAME = "myapp"
        CONTAINER_NAME = "myapp-container"
    }

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/your-repo.git'
            }
        }

        stage('Build') {
            steps {
                sh 'dotnet restore'
                sh 'dotnet build --configuration Release'
            }
        }

        stage('Test') {
            steps {
                sh 'dotnet test'
            }
        }

        stage('Publish') {
            steps {
                sh 'dotnet publish -c Release -o out'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t $IMAGE_NAME .'
            }
        }

        stage('Stop Old Container') {
            steps {
                sh 'docker stop $CONTAINER_NAME || true'
                sh 'docker rm $CONTAINER_NAME || true'
            }
        }

        stage('Run New Container') {
            steps {
                sh 'docker run -d -p 5000:80 --name $CONTAINER_NAME $IMAGE_NAME'
            }
        }
    }
}
```

---

# 🚀 7. Deploy on AWS EC2 (Real Production)

---

## Step 1: Launch EC2 (Ubuntu)

👉 Open ports:

* 22 (SSH)
* 8080 (Jenkins)
* 5000 (App)

---

## Step 2: Install Docker

```bash
sudo apt update
sudo apt install docker.io -y
```

---

## Step 3: Run Jenkins

```bash
docker run -d -p 8080:8080 jenkins/jenkins:lts
```

---

## Step 4: Connect GitHub + Jenkins

👉 Same webhook process

---

# 🚀 8. Auto Deploy Flow (FINAL)

```text
You push code →
GitHub →
Webhook →
Jenkins triggered →
Build + Test →
Docker image →
Container restarted →
Live app updated 🚀
```

---

# 🔥 Real Production Improvements

---

## ✅ Use Docker Hub (Optional)

```bash
docker tag myapp your-dockerhub/myapp
docker push your-dockerhub/myapp
```

---

## ✅ Use ENV Variables in Jenkins

```groovy
environment {
    DB_CONNECTION = credentials('db-conn')
}
```

---

## ✅ Zero Downtime Deploy (Advanced)

* Run new container
* Switch traffic
* Remove old container

---

# 💣 Common Issues

---

## ❌ Webhook not triggering

👉 Check:

* Public IP
* Firewall open

---

## ❌ Docker permission denied

```bash
sudo chmod 666 /var/run/docker.sock
```

---

## ❌ Jenkins can't access repo

👉 Use GitHub token

---

# 🧠 Interview Summary

* CI/CD = automation pipeline
* Jenkinsfile = pipeline definition
* Webhook = auto trigger
* Docker = deployment unit
* EC2 = hosting

---

# 🚀 You Are Now 🔥 Job Ready Backend Dev

You covered:

* Backend (.NET)
* Architecture
* Security (JWT)
* Performance (Redis)
* DevOps (Docker + Jenkins + AWS)

---

# 🚀 Next (If You Want)

👉 Kubernetes (K8s)
👉 Microservices
👉 System Design Interview
👉 Real SaaS project build

---

Just say 👇
**“Next: Kubernetes + Microservices”** 🚀
