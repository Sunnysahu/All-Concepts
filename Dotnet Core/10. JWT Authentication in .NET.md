---

# 🚀 JWT Authentication in .NET (Production Level)

---

# 🧠 What is JWT?

👉 JWT = **JSON Web Token**

👉 Used for:

```text
Authentication + Authorization (Stateless)
```

---

## 🔥 Flow (Very Important)

```text
User Login → Server validates → Generates JWT → Client stores token
→ Client sends token in every request → Server validates token
```

---

# 🧱 JWT Structure

```text
HEADER.PAYLOAD.SIGNATURE
```

---

## Example

```text
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

# ⚙️ Step 1: Install Package

```bash
dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer
```

---

# ⚙️ Step 2: Configure JWT (Program.cs)

```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.IdentityModel.Tokens;
using System.Text;

var key = Encoding.UTF8.GetBytes("your_secret_key_here");

builder.Services.AddAuthentication(options =>
{
    options.DefaultAuthenticateScheme = JwtBearerDefaults.AuthenticationScheme;
    options.DefaultChallengeScheme = JwtBearerDefaults.AuthenticationScheme;
})
.AddJwtBearer(options =>
{
    options.TokenValidationParameters = new TokenValidationParameters
    {
        ValidateIssuer = false,
        ValidateAudience = false,
        ValidateIssuerSigningKey = true,
        IssuerSigningKey = new SymmetricSecurityKey(key)
    };
});
```

---

# 🔌 Add Middleware

```csharp
app.UseAuthentication(); // MUST come before Authorization
app.UseAuthorization();
```

---

# 🧪 Step 3: Generate Token

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;

public string GenerateToken(string username)
{
    var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes("your_secret_key_here"));
    var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);

    var claims = new[]
    {
        new Claim(ClaimTypes.Name, username)
    };

    var token = new JwtSecurityToken(
        claims: claims,
        expires: DateTime.Now.AddHours(1),
        signingCredentials: creds
    );

    return new JwtSecurityTokenHandler().WriteToken(token);
}
```

---

# 📡 Step 4: Login API

```csharp
[HttpPost("login")]
public IActionResult Login(LoginDto dto)
{
    if (dto.Username == "admin" && dto.Password == "123")
    {
        var token = GenerateToken(dto.Username);
        return Ok(new { token });
    }

    return Unauthorized();
}
```

---

# 🔒 Step 5: Protect API

```csharp
[Authorize]
[HttpGet("secure")]
public IActionResult Secure()
{
    return Ok("This is protected");
}
```

---

# 📡 Client Request

```http
Authorization: Bearer YOUR_TOKEN_HERE
```

---

# 🧠 Claims (Important)

👉 Data stored inside token

```csharp
new Claim(ClaimTypes.Name, username),
new Claim("role", "admin")
```

---

# 🔥 Role-Based Authorization

```csharp
[Authorize(Roles = "admin")]
public IActionResult AdminOnly()
{
    return Ok();
}
```

---

# 💣 Common Mistakes

---

## ❌ Not using HTTPS

👉 Token can be stolen

---

## ❌ Storing secret key in code

👉 Use config/env variables

---

## ❌ Long expiry tokens

👉 Security risk

---

## ❌ Not validating token properly

👉 Anyone can access API

---

# 🔥 Best Practices (Production)

---

## ✅ Use appsettings.json

```json
"Jwt": {
  "Key": "super_secret_key",
  "ExpiryMinutes": 60
}
```

---

## ✅ Use Refresh Tokens (Advanced)

👉 Short-lived JWT + refresh token

---

## ✅ Store token safely

* Frontend → HTTP-only cookies (best)
* Avoid localStorage (XSS risk)

---

## ✅ Add more claims

* userId
* role
* permissions

---

# 🧠 Interview Questions

* What is JWT?
* Stateless vs session?
* What is claim?
* Difference between authentication & authorization?
* Why JWT over cookies?

---

# 🧠 Summary

* JWT = token-based auth
* Stateless → no session storage
* Use `[Authorize]` to protect APIs
* Claims = user data inside token
* Must use secure key + expiry

---

# 🚀 You Are Now 🔥 Industry Ready (Core Backend)

You covered:

* SOLID
* DI
* Middleware
* Filters
* EF Core
* Clean Architecture
* CQRS + MediatR
* JWT

---